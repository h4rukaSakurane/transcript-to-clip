現在開いているYouTubeアーカイブの文字起こしを基に、切り抜き候補を提示してください。

何らかの原因で文字起こし用のトランスクリプトが参照できなかった場合
以降の条件1〜7は実行せず、代わりに以下の内容だけを返してください。

「文字起こしが取得できなかったため、切り抜き候補は作成できませんでした。もしかしたら、概要欄下部にある“文字起こしを表示”ボタンを押したあとに再度実行すれば、文字起こしを取得できるかもしれません。」

条件：

1. 候補数は5〜10件

2. 各候補は以下をカラムにもった表形式にしてください
   - 連番（1スタート。outputファイル名に使用）
   - 始点〜終点（例：1:00〜1:30、1:02:00〜1:04:00）
   - 理由
   - タイトル案
   - 補足（なければ「なし」）

3. Premiereで最終トリムする前提なので、始点〜終点は前後10秒の余白込みで提示してください

4. その後、以下の関数を使ってffmpegで各候補だけを直接切り出すPowerShell用コマンドを準備してください

前提：

- New-Clip関数はPowerShellプロファイルで読み込み済み
- 入力ファイルは `$InputFile` 変数で指定する
- 出力先フォルダは `$OutDir` 変数で指定する
- `$InputFile` の初期値は以下とする
  `$env:USERPROFILE\Videos\4K Video Downloader+\${YouTubeの配信タイトル}.mp4`
- `$OutDir` の初期値は以下とする
  `C:\shared\generated-clips\${YouTubeの配信タイトル}`

New-Clip関数の呼び出し形式：
`New-Clip $InputFile $OutDir "開始時刻" "長さ" "出力ファイル名.mp4"`

5. PowerShellコマンドでは、始点〜終点からDurationを計算して `HH:MM:SS` 形式で指定してください

6. 出力ファイル名は `clip_01_内容.mp4` の形式にしてください
   - 内容部分は英数字・ハイフン・アンダースコアのみ
   - 日本語、空白、記号、絵文字は使わない

7. 最後に、PowerShellにコピペして実行できるコマンドブロックを出してください
   - `$InputFile = "..."`
   - `$OutDir = "..."`
   - 各 `New-Clip` 呼び出し
   - 最後に `Write-Host "clip generation completed."` を付ける
